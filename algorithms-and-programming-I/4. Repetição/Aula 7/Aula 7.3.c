//Imprimir os 7 primeiros números inteiros iniciando em 1 (usando o do-while).

#include <stdio.h>

main() {
	int num = 1;
	do {
		printf("\n%d", num);
		num++;
	}
	while(num < 8); {//Do-while faz o while levar vírgula porque o while é a parte composta da função.
	}
}
